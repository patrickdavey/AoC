export const part1 = (input) => {
  return chain(input)
        .thru(console.log)
        .value()
}

export const part2 = (input) => {
  return chain(input)
        .thru(console.log)
        .value()
}
